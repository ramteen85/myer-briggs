<div class="container">

    <div class="container__heading">
        <h2>Register your details</h2>
        <p>Welcome to the personality matchmaker. This test has been designed, so that you can calculate how well you would get along with your peers. Please register an account so we can keep track of your results. Otherwise, click the bottom link and log in to access your account.</p>
    </div>

    <% if @user.errors.any? %>
    <div class="error-box mar-bottom-1">   
            <p class="mar-bottom-1">Please correct the following errors:</p>
            <ul>
                <% @user.errors.full_messages.each do |e| %>
                <li><%= e %></li>
                <% end %>
            </ul>  
    </div>
    <% end %>
    
    
    <div class="form">
        <%= form_for @user do |f| %>
            <table style="width:100%">
                <tr>    
                    <td><%= f.label :username %></td>
                    <td><%= f.text_field :username %></td>
                </tr>
                <tr>
                    <td>Full Name:</td>
                    <td><%= f.text_field :fullname %></td>
                </tr>
                <tr>
                    <td>Your Age:</td>
                    <td><%= f.number_field :age, :value => 18 %></td>
                </tr>
                <tr>
                    <td>Your Gender:</td>
                    <td>
                        <%= f.select :gender, options_for_select([["Select One", ""],"Male", "Female"]) %>
                    </td>
                </tr>
                <tr>
                    <td>Country:</td>
                    <td><%= f.text_field :country %></td>
                </tr>
                <tr>
                    <td>State:</td>
                    <td><%= f.text_field :state %></td>
                </tr>
                <tr>
                    <td>City:</td>
                    <td><%= f.text_field :city %></td>
                </tr>
                <tr>
                    <td>Password:</td>
                    <td><%= f.password_field :password %></td>
                </tr>
                <tr>
                    <td>Confirm Password:</td>
                    <td><%= f.password_field :password_confirmation %></td>
                </tr>
            </table>
            <%= f.submit 'Register', :class => "btn mar-top-1" %>
        <% end %>
    </div>
    <a href="/login">Already Have an account? Sign in here.</a>
</div>